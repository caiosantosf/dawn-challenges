/*
Is it a rotation?
This challenge was asked recently in Apple interviews:

Given two strings A and B, return true if string A is a rotation of string B.

Example:
string A = "abcde"
string B = "cdeab"

String A is a rotation of B, because A can be rotated 3 times to the right until it is equivalent to B.

Function to complete:
function isRotation (a, b) {
    // complete function
}

console.log ("is a rotation:" + isRotation('abcde', 'cdeab')); // true
*/

function isRotation(a, b) {
  let arrA = a.split('')
  //arrA.join('')
}

isRotation('abcde', 'cdeab')

//console.log ("is a rotation:" + isRotation('abcde', 'cdeab'))

arr.push(arr.shift())